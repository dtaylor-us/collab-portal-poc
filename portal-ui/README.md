# Planning Results Portal UI

This directory marks the boundary for a future custom Portal user experience. No frontend framework has been selected, so this increment intentionally contains no generated frontend application.

The Portal will call capability APIs; it will not own the complete comments, corrections, dispositions, study files, or workflow state.
