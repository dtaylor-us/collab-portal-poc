# Workflows

This directory contains the existing Camunda Desktop Modeler artifacts. The BPMN content and forms are preserved as authored. Form references are deployed form IDs, so the BPMN and forms should be deployed together to the same Camunda 8.9 environment.

Application modules do not auto-deploy these resources; use Desktop Modeler as described in the root README.
